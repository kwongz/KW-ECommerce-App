

function Cart( {cartItems} ) {

    console.log(cartItems)

    return (
        <>
            <ul>
                {/* {
                    cartItems.map((item) => {
                        return(
                            <li>
                                <p>{item.title}</p>
                            </li>
                        )
                    })
                } */}
            </ul>
        </>
    )
}

export default Cart
